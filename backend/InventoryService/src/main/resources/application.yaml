spring:
  application:
    name: InventoryService

  mongodb:
      uri: mongodb+srv://pyjpallegama_db_user:L6RH88hyuwihvrj3@inventory-service-clust.m01s45s.mongodb.net/inventorydb?appName=Inventory-Service-Cluster
  Jackson:
    serialization:
      indent_output: true

server:
  port: 8081

logging:
  level:
    root: INFO
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG